export const lowercase =(str)=>{
  if (!str) return ''
  str = str.toString()
  return str.toLowerCase()
}

export const uppercase =(str)=>{
  if (!str) return ''
  str = str.toString()
  return str.toUpperCase()
}